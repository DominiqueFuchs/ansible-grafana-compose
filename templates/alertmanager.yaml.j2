route:
  receiver: '{{ lc_alertmanager_default_receiver }}'
  group_by: [ alertname ]

receivers:
{% if lc_alertmanager_receivers %}
{# to_nice_json because PyYAML is a freaking mess w/ dict lists and offsets #}
  {{ lc_alertmanager_receivers | to_nice_json }}
{% else %}
  - name: 'black-hole'
{% endif %}
